name: PHPUnit
on: push

jobs:
  run:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: ['ubuntu-latest']
        php-versions: ['8.2', '8.3', '8.4']
        testbench-versions: ['^9.9', '^10.0']

    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-versions }}
          tools: phpunit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: composer require --dev orchestra/testbench:${{ matrix.testbench-versions }}
      - run: vendor/bin/testbench workbench:install --no-interaction
      - run: ln -s vendor/bin/testbench ./artisan
      - run: phpunit --filter testSuccessfulRequestEvents$ --no-coverage
      - run: cat ./tests/Storage/*
      - run: which curl
      # - run: "curl --write-out 'requestid_12 10 30 %{time_connect} %{time_total} %{exitcode} %{response_code}' --connect-timeout 10 --max-time 30 --output /dev/null --silent --location --header 'Content-Type: application/json' --header 'Accept: application/json' --request 'get' --data '' 'https://httpbin.org/get'"
      # - run: "(curl --write-out 'requestid_12 10 30 %{time_connect} %{time_total} %{exitcode} %{response_code}' --connect-timeout 10 --max-time 30 --output /dev/null --silent --location --header 'Content-Type: application/json' --header 'Accept: application/json' --request 'get' --data '' 'https://httpbin.org/delay/3' | xargs php artisan http-background:request-callback) > /dev/null 2>&1 & echo $!"
      - run: "curl --write-out '%{response_code}' --connect-timeout 10 --max-time 30 --output /dev/null --silent --location --header 'Content-Type: application/json' --header 'Accept: application/json' --request 'get' --data '' 'https://httpbin.org/get'"
      - run: "curl --connect-timeout 10 --max-time 30 --location --header 'Content-Type: application/json' --header 'Accept: application/json' --request 'get' --data '' 'https://httpbin.org/get'"
      - run: "curl --write-out '%{response_code}' --connect-timeout 10 --max-time 30 --output /dev/null --silent --location --header 'Content-Type: application/json' --header 'Accept: application/json' --request 'get' 'https://httpbin.org/get'"
      - run: "curl --connect-timeout 10 --max-time 30 --location --header 'Content-Type: application/json' --header 'Accept: application/json' --request 'get' 'https://httpbin.org/get'"
      # - run: ps aux
