name: windows
on: push

jobs:
  run:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: ['windows-latest']
        php-versions: ['8.4']
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-versions }}
          tools: phpunit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: which curl
      - run: which xargs
      - run: curl --help all
      - run: "curl --write-out 'requestid_123 10 30 %{time_connect} %{time_total} %{exitcode} %{response_code}' --connect-timeout 10 --max-time 30 --output /dev/null --silent --location --header 'Content-Type: application/json' --header 'Accept: application/json' --request 'get' --data '' 'https://httpbin.org/get'"
      - run: php -r 'var_dump(PHP_OS);'
      - run: tasklist /?
      - run: tasklist

      # - run: composer require --dev orchestra/testbench
      # - run: vendor/bin/testbench workbench:install --no-interaction
      # - run: ln -s vendor/bin/testbench ./artisan
      # - run: phpunit --filter testGet$
      # - run: cat vendor/orchestra/testbench-core/laravel/storage/logs/laravel.log

      # - name: Make code coverage badge
      #   uses: timkrase/phpunit-coverage-badge@v1.2.1
      #   with:
      #     coverage_badge_path: output/coverage.svg
      #     push_badge: false
      # - name: Git push to image-data branch
      #   uses: peaceiris/actions-gh-pages@v3
      #   with:
      #     publish_dir: ./output
      #     publish_branch: image-data
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     user_name: 'github-actions[bot]'
      #     user_email: 'github-actions[bot]@users.noreply.github.com'
